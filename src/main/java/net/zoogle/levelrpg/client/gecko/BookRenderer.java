package net.zoogle.levelrpg.client.gecko;

/**
 * Placeholder retained for future 4.6.x GUI renderer. Currently unused to avoid API mismatch errors.
 * Rendering is handled by the vanilla fallback path in LevelBookScreen.
 */
/**
 * Placeholder retained for future 4.6.x GUI renderer. Currently unused to avoid API mismatch errors.
 * Rendering is handled by the vanilla fallback path in LevelBookScreen.
 */
public class BookRenderer {
}
